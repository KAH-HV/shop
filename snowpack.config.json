{
  "scripts": {
    "mount:_site": "mount src/dist --to /",
    "mount:js": "mount src/site/_includes/js --to /_dist_",
    "mount:web_modules": "mount web_modules",
    "run:11ty": "eleventy",
    "run:11ty::watch": "$1 --watch"
  },
  "installOptions": {
    "NODE_ENV": true
  },
  "devOptions": {
    "out": "dist"
  }
}
